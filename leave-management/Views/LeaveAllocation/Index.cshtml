@model leave_management.Models.CreateLeaveAlloactionVM

@{
    ViewData["Title"] = "Index";
   // Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>


<table class="table">
    <thead>
        <tr>
            <th>
                Leave Types
            </th>

            <th>Action</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.leaveTypes) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>

            <td>
                @*@Html.ActionLink("Allocate To Employees", "SetLeave", new { id = item.Id })  <!-- Call SetLeave action and pass id value to it.--> *@
                <a class="btn btn-outline-success" asp-action="SetLeave" asp-route-id="@item.Id">Allocate To Employees</a> <!-- Call SetLeave action and pass id value to it.-->
            </td>
        </tr>
}
    </tbody>
</table>
